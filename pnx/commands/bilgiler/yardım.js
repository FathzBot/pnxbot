const Discord = require("discord.js");
const config = require("../../config.json")

module.exports = {
    name: "yardım",
    category: "bilgiler",
    description: "s",
    run: async (bot, message, args) => {

    }
}