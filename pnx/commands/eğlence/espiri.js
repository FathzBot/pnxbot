const Discord = require("discord.js");
const config = require("../../config.json")

module.exports = {
    name: "espiri",
    category: "eğlence",
    description: "s",
    run: async (bot, message, args) => {



    }
}
