const Discord = require("discord.js");
const config = require("../../config.json")

module.exports = {
    name: "hentaii",
    category: "animeler",
    description: "s",
    run: async (bot, message, args) => {
 

 
    }
}